version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
   
    commands:
        - npm install
       
  build:
    commands:
        - npm run build:prod
     
artifacts:
  files:
    - public/**/*
    - src/**/*
    - package.json
    - appspec.yml
    - scripts/**/*